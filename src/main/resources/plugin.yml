name: ${project.name}
version: ${project.version}
description: ${project.description}
author: MrZoraman
website: NULL
main: ${project.groupId}.multihomes.${project.artifactId}
prefix: Homes
depend: [${build.plugin.dependency}]
commands:
  home:
  sethome:
  delhome:
